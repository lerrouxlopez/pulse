<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>Pulse - Team Profile</title>
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Space+Grotesk:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN"
      crossorigin="anonymous"
    />
    <link rel="stylesheet" href="/static/site.css" />
  </head>
  <body class="bg-ink text-cream dashboard-body">
    <div class="dashboard-wrap">
      <aside class="sidebar">
        {% include "partials/sidebar" %}
      </aside>

      <main class="content">
        <header class="d-flex flex-wrap align-items-center justify-content-between mb-4 gap-3">
          <div>
            <h1 class="fw-bold mb-1">{{ team.name }}</h1>
            <p class="text-cream-75 mb-0">Tournament: {{ tournament_name }}</p>
          </div>
          <a class="btn btn-outline-light" href="/{{ tournament_slug }}/teams">Back to Teams</a>
        </header>

        <section class="panel mb-4">
          <div class="d-flex align-items-center gap-3">
            {% if team.logo_url %}
            <img src="{{ team.logo_url }}" alt="Team logo" style="width: 64px; height: 64px; object-fit: cover; border-radius: 14px;">
            {% else %}
            <div class="bg-light text-dark d-flex align-items-center justify-content-center" style="width: 64px; height: 64px; border-radius: 14px;">?</div>
            {% endif %}
            <div>
              <div class="fw-semibold">{{ team.name }}</div>
              <div class="text-cream-60 small">{{ team.members | length }} members</div>
            </div>
          </div>
        </section>

        <section class="panel mb-4">
          <h3 class="fw-semibold mb-3">Entered Divisions</h3>
          <div class="d-flex flex-wrap gap-2">
            {% for division in team.divisions %}
            <span class="badge text-bg-light text-ink">{{ division.name }}</span>
            {% else %}
            <span class="text-cream-60">None</span>
            {% endfor %}
          </div>
        </section>

        <section class="panel mb-4">
          <h3 class="fw-semibold mb-3">Entered Categories</h3>
          <div class="d-flex flex-wrap gap-2">
            {% for category in team.categories %}
            <span class="badge text-bg-light text-ink">{{ category.name }}</span>
            {% else %}
            <span class="text-cream-60">None</span>
            {% endfor %}
          </div>
        </section>

        <section class="panel mb-4">
          <h3 class="fw-semibold mb-3">Entered Events</h3>
          <div class="d-flex flex-wrap gap-2">
            {% for event in team.events %}
            <span class="badge text-bg-light text-ink">{{ event.name }}</span>
            {% else %}
            <span class="text-cream-60">None</span>
            {% endfor %}
          </div>
        </section>

        <section class="panel">
          <h3 class="fw-semibold mb-3">Members</h3>
          <div class="d-flex flex-wrap gap-2">
            {% for member in team.members %}
            <span class="badge text-bg-light text-ink">{{ member.name }}</span>
            {% else %}
            <span class="text-cream-60">No members yet.</span>
            {% endfor %}
          </div>
        </section>
      </main>
    </div>

    <script
      src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
      integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
      crossorigin="anonymous"
    ></script>
  </body>
</html>
